<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lis.mapper.SysZxtmcMapper">

    <select id="listSystems" resultType="map">
        SELECT zxtid, zxtjc, zxtmc, zxtbb, bz FROM sys_zxtmc ORDER BY zxtid
    </select>

</mapper>
